1) Create view nombre_cliente as select * from clientes
2)insert into nombre cliente ('Antonela','Rojas')
3)drop view nombre_cliente
4)create view Productos_bic as select marca,descripcion from productos where marca = 'BIC'
5)CREATE VIEW porteños AS SELECT * FROM Clientes WHERE Provincia = 'buenos aires' OR Ciudad = 'ciudad autonoma';
6)DROP VIEW porteños;
CREATE VIEW porteños AS SELECT * FROM Clientes WHERE Provincia = 'buenos aires';
7)CREATE VIEW productos_precio AS SELECT * FROM Productos WHERE precio > 100;
8)create view cliente_ciudad as select clientes.nombre, clientes.apellido, ciudades.nombre from clientes join ciudades on clientes.localidad = ciudad.id;
9)create view ciudad_provincia as select ciudades.nombre, provincias.nombre from ciudades join provincias on ciudad.provincia = provincia.id;
10)create view ultima_compra as select clientes.nombre, compras.fecha from clientes join compras on clientes.id = compras.cliente order by compras.fecha desc limit 1;
